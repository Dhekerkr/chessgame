{
  "name": "projet-vue",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --reporter=html --outputFile=html/unit/index.html --pool=vm",
    "test:run": "vitest run --reporter=html --outputFile=html/unit/index.html --pool=vm",
    "test:e2e": "playwright test --reporter=html --output=html/playwright"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vitest/ui": "^2.1.9",
    "vite": "^7.2.4",
    "vitest": "^2.1.8"
  }
}
